#liste de mot 
l = ["Salut", "alors", "ça", "va", "ma", "grosse", "puce","yellow", "?"]
#renvoie une nouvelle liste contenant la longueur de chaque mot
def newlist(i):
   for i in l:
         print(len(i)):
newlist(l)
